<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/schedule_widget"

    android:minWidth="250dp"
    android:minHeight="150dp"

    android:minResizeWidth="250dp"
    android:minResizeHeight="150dp"

    android:resizeMode="horizontal|vertical"

    android:configure="ttgt.schedule.WidgetSettings"
    android:widgetFeatures="configuration_optional|reconfigurable"
    android:updatePeriodMillis="3600000"
    android:previewImage="@mipmap/widget_preview"
    />